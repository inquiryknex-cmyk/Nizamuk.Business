# صفحات البيع المباشر — نظام إعادة البناء

مرجع المطوّر لصفحات `/rebuild/…` التي حلّت محلّ صفحات `/google/…` القديمة.
مصدر المحتوى: `NizamOk_Google_Rebuild_Landing_Pages_Master.md` (النسخة 1.0، 26 يوليو 2026).

## 1. الصفحات الأربع

| الرابط | النمط | صفحات المنتج | رابط Dodo | الغلاف |
|---|---|---:|---|---|
| `/rebuild/mubdia/` | المبدعة المشتّتة | 47 | `dodo.pe/rebuild-mubdia-inter` | `rebuild-mubdia.jpg` |
| `/rebuild/asirat/` | أسيرة الكمال | 47 | `dodo.pe/rebuild-asirat-inter` | `rebuild-asira.jpg` |
| `/rebuild/kafua/` | الكفؤة المنهكة | 47 | `dodo.pe/rebuild-munhaka-inter` | `rebuild-kafua.jpg` |
| `/rebuild/mutafadia/` | المتفادية الذكية | 46 | `dodo.pe/rebuild-mutafadia-inter` | `rebuild-mutafadiya.jpg` |

انتبهي إلى اختلاف التسميات المتعمَّد: رابط الصفحة `asirat` بينما اسم ملف الغلاف
`asira`؛ ورابط الصفحة `mutafadia` بينما الغلاف `mutafadiya`، ومفتاح النمط في
`config.js` هو `mutafadiya`. روابط Dodo أعلاه مطابقة لما في
`site/assets/js/config.js` → `products.<pattern>.rebuild`، وهي **مكتوبة مباشرة**
في `href` كل زر حتى يعمل الشراء ولو تعطّل JavaScript. عند تغيير أي رابط في Dodo
يجب تحديث الموضعين معًا.

لا توجد صفحة اختيار تجمع الأنماط الأربعة. كل رابط = رسالة واحدة، منتج واحد،
قرار واحد. هذا مقصود ولا يجوز دمجه.

## 2. الملفات

```
site/rebuild/<slug>/index.html   الصفحة الكاملة (15 قسمًا)، ثابتة وقابلة للزحف
site/assets/css/rebuild.css      تخطيط صفحات البيع فقط — يبني على main.css
site/assets/js/rebuild.js        القياس + الشريط الثابت + المعرض + lightbox
site/shukran/index.html          صفحة ما بعد الدفع (noindex)
```

بنية الصفحات متطابقة سطرًا بسطر؛ الاختلاف في النسخة فقط. عند تعديل بنية قسم،
عدّليه في الصفحات الأربع حتى لا تتفرّق القوالب.

## 3. ترتيب الأقسام

رأس بسيط · Hero · مرآة التعرف · إعادة التأطير · الآلية · ماذا تستلمين ·
الأدوات الخمس · قلب النظام · معرض المنتج · رحلة التطبيق · النتائج المحتملة ·
لمن يناسب · القيمة والسعر · الثقة والحدود · الأسئلة الشائعة · CTA النهائي · التذييل.

نقاط القرار خمس: Hero، إعادة التأطير، بعد الآلية، بطاقة الشراء، CTA النهائي —
بالإضافة إلى الشريط الثابت على الهاتف. لا تضيفي أزرارًا كل بضعة أسطر.

## 4. المعرض — صور المنتج الحقيقية

المعرض **مكتمل**: ست صور لكل صفحة، كلها ملتقطة من ملفات الأنظمة نفسها
(`Nizam_*_v2.html`) التي تصل إلى المشترية — لا صور تجريبية ولا لوحة تحكم مستقبلية.

| النمط | الصفحات المعروضة |
|---|---|
| المبدعة المشتّتة | الغلاف · 7 خريطة المنعطفات · 8 خريطة العبور · 23 تجربة الوصول · 33 خطة ٣٠ · 36 قوس ٩٠ |
| أسيرة الكمال | الغلاف · 5 الحراسة الثلاثية · 6 خريطة الإغلاق · 20 محكمة التعديل الأخير · 31 تحدّي ٧ أيام · 35 قوس ٩٠ |
| الكفؤة المنهكة | الغلاف · 7 عقد الاعتماد · 8 خريطة الفسخ · 23 تجربة الكشف · 33 خطة ٣٠ · 36 قوس ٩٠ |
| المتفادية الذكية | الغلاف · 6 المواجهات الأربع · 7 خريطة الباب المؤجّل · 22 الأداة التوقيعية · 32 خطة ٣٠ · 35 قوس ٩٠ |

لكل صفحة ملفان في `site/assets/product/<slug>/`:

- `pNN.webp` — الصفحة كاملة بعرض 1400px، تُفتح في الـ lightbox.
- `pNN-thumb.webp` — قصّة علوية بنسبة 3:4 وعرض 620px للبطاقة المصغّرة.

سبب الملفين: صفحات المنتج طويلة (نحو 1:1.6). لو قُصّت البطاقة من الصورة الكاملة
لقُطع النص العربي من الجانبين. البطاقة تعرض عرض الصفحة كاملًا مقصوصًا من الأسفل،
والـ lightbox يمرّر الصفحة كاملة بعرض 820px حتى يبقى النص مقروءًا.

قسم المعرض يبقى **مخفيًا** حتى يتأكد `rebuild.js` من تحميل صورة واحدة على الأقل،
فلا يظهر معرض فارغ أو صور مكسورة إذا نقص ملف.

### إعادة توليد الصور

الملفات المصدر ليست داخل المستودع (نحو 19 ميغابايت). عند تحديث أي نظام:

1. افتحي ملف النظام في Chromium. المحتوى يُفكّ عبر React من `unpkg.com`؛ إن كان
   محجوبًا، حمّلي `react` و`react-dom` UMD 18.3.1 محليًا واعترضي الطلب.
2. كل `<section>` = صفحة واحدة، بالترتيب. رقم الصفحة = ترتيب القسم.
3. التقطي القسم المطلوب بعرض 1200 وdeviceScaleFactor 2.
4. صدّري `pNN.webp` بعرض 1400 وجودة 80، و`pNN-thumb.webp` بعرض 620 مقصوصة 3:4 من الأعلى.

## 5. القياس

الأحداث تمر عبر `trackEvent` في `analytics.js` (Zaraz ← GA4 ← صامت).

| الحدث | متى | ملاحظة |
|---|---|---|
| `view_landing_page` | مرة واحدة عند الفتح | مع `pattern_slug` ومعلمات UTM و`has_gclid` |
| `view_product_preview` | أول فتح لصورة من المعرض | — |
| `begin_checkout` | ضغط أي زر شراء | نيّة فقط، ليست إيرادًا |
| `quiz_fallback_click` | ضغط رابط الاختبار الثانوي | — |
| `scroll_50` / `scroll_90` | عمق التمرير | تشخيصي، ليس تحويلًا تجاريًا |

**حدث `purchase` غير مُطلَق من هذا الموقع، عن قصد.** النقر على Dodo ليس شراءً،
ووجود معلمة في رابط `/shukran/` ليس دليل دفع — الصفحة يمكن تحديثها أو فتحها
مباشرة. يجب أن يأتي `purchase` بقيمة `109` وعملة `SAR` و`transaction_id` حقيقي
من تكامل Dodo مع GA4/GTM أو من الـ webhook، مع منع التكرار عند التحديث.

### ما يبقى مطلوبًا خارج المستودع

- [ ] GA4/GTM غير مثبّتين على الموقع بعد (`analytics.js` يفشل بصمت بلا مزوّد).
- [ ] استخدام نفس معرّف GA4/GTM في الموقع وفي Dodo لاستمرار القياس عبر النطاق.
- [ ] توجيه Dodo بعد الدفع إلى `https://nizamok.com/shukran/`.
- [ ] تفعيل حدث الشراء في Dodo والتحقق من القيمة والعملة ورقم العملية.
- [ ] عملية اختبار كاملة ثم عملية حقيقية منخفضة المخاطر قبل تشغيل الميزانية.

## 6. قواعد لا يجوز كسرها

- CTA أساسي واحد: **احصلي على النظام — 109 ر.س**. الاختبار رابط نصّي ثانوي فقط.
- السعر ظاهر فوق الطية، عند القيمة، في CTA الأخير، وفي الشريط الثابت.
- الدفع يفتح في النافذة نفسها (لا `target="_blank"`).
- لا عدّاد زائف، لا خصم غير حقيقي، لا «بقيت نسختان»، ولا شهادات أو أرقام مبيعات.
- لا «علاج» ولا «تشخيص» ولا «نتيجة مضمونة» ولا «مبني علميًا». الصيغة: «يساعدكِ على».
- لا لمحات ولا جذور ولا لوحة الانتظار على هذه الصفحات — قرار واحد لكل صفحة.
- Product schema ببيانات حقيقية فقط. **ممنوع** إضافة `review` أو `aggregateRating`.
- عنوان H1 واحد لكل صفحة، والأقسام H2.

## 7. قائمة فحص ما قبل تشغيل الحملة

- [ ] كل صفحة لها H1 واحد ورسالة مطابقة لعبارات الإعلان.
- [ ] السعر 109 ر.س ظاهر فوق الطية وعند كل CTA.
- [ ] كل زر شراء يذهب إلى رابط النمط الصحيح، ومُختبَر من هاتف سعودي.
- [ ] **طريقة الوصول بعد الدفع مكتوبة بدقة ومُختبَرة** — النص الحالي في الأسئلة
      الشائعة و`/shukran/` يقول «تظهر تعليمات الوصول ويصل تأكيد على بريد الدفع»؛
      يجب أن يطابق ما يحدث فعلًا في Dodo قبل النشر، وإلا يُعاد صياغته.
- [x] **أرقام 46/47 صفحة و5 مراحل و5 أدوات مطابقة للملفات المسلَّمة** — تم التحقق
      2026-07-26 مقابل ملفات `Nizam_*_v2.html`؛ التفاصيل في §10.
- [x] صور المعرض من المنتج الحقيقي وليست من لوحة التحكم المستقبلية.
- [ ] الخصوصية والشروط والاسترداد والدعم ظاهرة وتعمل.
- [ ] GA4/GTM يعملان على الموقع وDodo، وحدث purchase يمر بقيمة SAR ورقم عملية.
- [ ] لا يتكرر purchase عند التحديث أو الرجوع.
- [ ] اختبار السرعة وRTL والخطوط والأزرار وفتح الصور على iPhone/Android وسطح المكتب.
- [ ] لا وعود علاجية أو تشخيصية أو شهادات وأرقام مختلقة.
- [ ] الاختبار المجاني ثانوي بصريًا ولا ينافس زر الشراء.
- [ ] حفظ نسخة أساسية للصفحات قبل بدء أي A/B test.

## 8. ترتيب الإطلاق

الصفحات الأربع جاهزة، لكن الميزانية تبدأ بصفحة **المبدعة المشتّتة** وحدها ضمن
الاختبار التشخيصي الأول. ثم أسيرة الكمال، ثم المتفادية الذكية، ثم الكفؤة المنهكة
(نيّتها أوسع وأكثر حساسية). لا توزّعي ميزانية الأسبوع الأول على أربع صفحات.

نجاح الحملة يُقاس بعملية شراء 109 ر.س — لا بالنقر على Dodo ولا ببدء الاختبار.

## 9. الصفحات المتقاعدة

`/google/not-finishing/` و`/google/procrastination/` حُذفتا. إعلاناتهما كانت تَعِد
باختبار مجاني، لذلك تُحوَّل الروابط القديمة (301) إلى `/ikhtibar/` وليس إلى صفحات
البيع — انظري `site/_redirects`. وجّهي الحملات الجديدة إلى `/rebuild/…` مباشرة.

## 10. تدقيق الادعاءات مقابل ملفات المنتج (2026-07-26)

المصدر: `Nizam_Mubdia_AlMushattata_v2.html` · `Nizam_Asirat_AlKamal_v2.html` ·
`Nizam_Kafuaa_AlMunhaka_v2.html` · `Nizam_Mutafadiya_AlThakiya_v2.html`.
كل `<section>` في الملف = صفحة واحدة في المنتج.

### عدد الصفحات — مطابق

| النمط | ما تقوله الصفحة | ما في الملف |
|---|---:|---:|
| المبدعة المشتّتة | 47 | 47 ✅ |
| أسيرة الكمال | 47 | 47 ✅ |
| الكفؤة المنهكة | 47 | 47 ✅ |
| المتفادية الذكية | 46 | 46 ✅ |

### «خمس مراحل تبدأ بفك شيفرة النمط وتنتهي بخطة عودة بعد التعثر» — مطابق

خمس مراحل معنونة صراحةً في الأنظمة الأربعة، الأولى دائمًا «فكّ شيفرة النمط»
(صفحة 2)، والثانية دائمًا «إيقاف النزيف»، والخامسة خطة العودة/منع الانتكاس:

| النمط | ١ | ٢ | ٣ | ٤ | ٥ |
|---|---|---|---|---|---|
| مبدعة | 2 | 13 | 27 | 31 | 37 عودي بسرعة، بلا قصة فشل |
| أسيرة | 2 | 11 | 25 | 29 | 36 منع الانتكاس |
| كفؤة | 2 | 13 | 27 | 31 | 37 عودي بسرعة، بلا قصة فشل |
| متفادية | 2 | 12 | 26 | 30 | 36 عودي بسرعة، بلا قصة فشل |

### الأدوات الخمس — الأسماء مطابقة حرفيًا

كل أداة لها صفحتها المستقلة داخل المنتج: مبدعة 18–22 · أسيرة 15–19 ·
كفؤة 18–22 · متفادية 17–21.

### الآليات والتجارب التوقيعية — مطابقة

خريطة المنعطفات الأربعة (7) وخريطة العبور (8) وتجربة الوصول (23–24) ·
الحراسة الثلاثية (5) وخريطة الإغلاق (6) ومحكمة التعديل الأخير (20–21) ·
عقد الاعتماد (7) وخريطة الفسخ (8) وتجربة الكشف (23–24) ·
المواجهات الأربع (6) وخريطة الباب المؤجّل (7) والأداة التوقيعية (22–23).

### تصحيحات طُبّقت على النصوص

- الكفؤة: «عقد الاعتماد — الصفحتان 7–8» ← **صفحة 7**؛ خريطة الفسخ هي صفحة 8.
- أسيرة: «مسار 90 يومًا — الصفحات 34–35» ← **صفحة 35** (قوس ٩٠ يومًا)؛
  صفحة 34 هي «متابعة الشهر».
- عناوين المعرض صارت تطابق عنوان الصفحة داخل المنتج كلمةً بكلمة.

### ما زال غير محسوم

صيغة التسليم. الملفات المرفوعة صفحات HTML مجمّعة تحتاج JavaScript لعرضها،
وليست PDF. نص «كيف يصلني النظام؟» في الصفحات لا يذكر الصيغة — وهذا مقصود حتى
تتأكد طريقة التسليم الفعلية في Dodo. **لا تنشري الحملة قبل ضبط هذه الصياغة.**
