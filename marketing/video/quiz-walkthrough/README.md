# NizamOk — «داخل الاختبار» quiz walkthrough video

Production-ready 9:16 walkthrough (1080×1920, 30fps, 22.0s) that pulls the viewer
inside the real NizamOk quiz — built beat-for-beat from the recorded session of
2026-07-25, for TikTok first, then Reels/Shorts.

- `deliverables/` — master (overlays+VO), clean (no text), no-VO, poster frame.
- `docs/` — timestamped storyboard, sound-design timeline, production report
  (authenticity decisions, Higgsfield usage, fallbacks), final-cut contact sheet.
- `project/` — everything needed to re-render: capture rig for the deployed quiz,
  pinned take order, compositor, mix, typography sources, all VO takes.

Publish notes: caption CTA is «الرابط في البايو» — keep the quiz link
(`nizamok.com/ikhtibar/` or the `/q` short link) in bio before posting.
